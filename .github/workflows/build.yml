name: NKPlayer - Background Playback (v1.0.52)
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.19.0'

      - name: Generate NKPlayer Project
        run: |
          flutter create --org com.nareshkushmi --project-name nkplayer .

          sed -i 's/android:label="nkplayer"/android:label="NKPlayer"/' android/app/src/main/AndroidManifest.xml
          sed -i 's/targetSdkVersion .*/targetSdkVersion 36/' android/app/build.gradle || true
          sed -i 's/minSdkVersion .*/minSdkVersion 21/' android/app/build.gradle || true

          # ── pubspec.yaml ──────────────────────────────────────────────────────
          cat <<'PUBSPEC' > pubspec.yaml
          name: nkplayer
          description: NKPlayer Pro - Accessible Media Player with Background Playback.
          version: 1.0.52+53
          publish_to: 'none'
          environment:
            sdk: '>=3.0.0 <4.0.0'
          dependencies:
            flutter:
              sdk: flutter
            audioplayers: ^5.2.1
            on_audio_query: ^2.9.0
            permission_handler: ^11.3.0
            google_fonts: ^6.1.0
            device_info_plus: ^10.1.0
          flutter:
            uses-material-design: true
          PUBSPEC

          # ── AndroidManifest.xml ───────────────────────────────────────────────
          python3 -c "
          path = 'android/app/src/main/AndroidManifest.xml'
          with open(path, 'r') as f:
              content = f.read()

          perms = '''    <uses-permission android:name=\"android.permission.WRITE_EXTERNAL_STORAGE\"/>
              <uses-permission android:name=\"android.permission.READ_EXTERNAL_STORAGE\"/>
              <uses-permission android:name=\"android.permission.MANAGE_EXTERNAL_STORAGE\"/>
              <uses-permission android:name=\"android.permission.READ_MEDIA_AUDIO\" />
              <uses-permission android:name=\"android.permission.POST_NOTIFICATIONS\" />
              <uses-permission android:name=\"android.permission.FOREGROUND_SERVICE\" />
              <uses-permission android:name=\"android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK\" />\n'''

          service = '''
              <service
                  android:name=\".MusicService\"
                  android:foregroundServiceType=\"mediaPlayback\"
                  android:exported=\"false\" />\n'''

          content = content.replace('<application', perms + '    <application android:requestLegacyExternalStorage=\"true\"')
          content = content.replace('</application>', service + '    </application>')
          with open(path, 'w') as f:
              f.write(content)
          "

          # ── MusicService.kt (Native Android Foreground Service) ───────────────
          mkdir -p android/app/src/main/kotlin/com/nareshkushmi/nkplayer
          cat <<'KT' > android/app/src/main/kotlin/com/nareshkushmi/nkplayer/MusicService.kt
          package com.nareshkushmi.nkplayer

          import android.app.*
          import android.content.Intent
          import android.os.Build
          import android.os.IBinder
          import androidx.core.app.NotificationCompat

          class MusicService : Service() {

              companion object {
                  const val CHANNEL_ID = "nkplayer_channel"
                  const val NOTIFICATION_ID = 1
                  const val ACTION_START = "ACTION_START"
                  const val ACTION_STOP = "ACTION_STOP"
                  const val EXTRA_TITLE = "song_title"
                  const val EXTRA_ARTIST = "song_artist"
              }

              override fun onBind(intent: Intent?): IBinder? = null

              override fun onCreate() {
                  super.onCreate()
                  createNotificationChannel()
              }

              override fun onStartCommand(intent: Intent?, flags: Int, startId: Int): Int {
                  when (intent?.action) {
                      ACTION_STOP -> {
                          stopForeground(true)
                          stopSelf()
                          return START_NOT_STICKY
                      }
                      else -> {
                          val title = intent?.getStringExtra(EXTRA_TITLE) ?: "NKPlayer"
                          val artist = intent?.getStringExtra(EXTRA_ARTIST) ?: "Unknown"
                          startForeground(NOTIFICATION_ID, buildNotification(title, artist))
                      }
                  }
                  return START_STICKY
              }

              private fun buildNotification(title: String, artist: String): Notification {
                  return NotificationCompat.Builder(this, CHANNEL_ID)
                      .setContentTitle(title)
                      .setContentText(artist)
                      .setSmallIcon(android.R.drawable.ic_media_play)
                      .setOngoing(true)
                      .setPriority(NotificationCompat.PRIORITY_LOW)
                      .build()
              }

              private fun createNotificationChannel() {
                  if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.O) {
                      val channel = NotificationChannel(
                          CHANNEL_ID,
                          "NKPlayer",
                          NotificationManager.IMPORTANCE_LOW
                      ).apply {
                          description = "NKPlayer music playback"
                          setSound(null, null)
                      }
                      val manager = getSystemService(NotificationManager::class.java)
                      manager.createNotificationChannel(channel)
                  }
              }
          }
          KT

          # ── MainActivity.kt ───────────────────────────────────────────────────
          cat <<'KT' > android/app/src/main/kotlin/com/nareshkushmi/nkplayer/MainActivity.kt
          package com.nareshkushmi.nkplayer

          import android.content.Intent
          import android.os.Build
          import io.flutter.embedding.android.FlutterActivity
          import io.flutter.embedding.engine.FlutterEngine
          import io.flutter.plugin.common.MethodChannel

          class MainActivity : FlutterActivity() {
              private val CHANNEL = "com.nareshkushmi.nkplayer/music_service"

              override fun configureFlutterEngine(flutterEngine: FlutterEngine) {
                  super.configureFlutterEngine(flutterEngine)
                  MethodChannel(flutterEngine.dartExecutor.binaryMessenger, CHANNEL)
                      .setMethodCallHandler { call, result ->
                          when (call.method) {
                              "startService" -> {
                                  val title = call.argument<String>("title") ?: ""
                                  val artist = call.argument<String>("artist") ?: ""
                                  val intent = Intent(this, MusicService::class.java).apply {
                                      action = MusicService.ACTION_START
                                      putExtra(MusicService.EXTRA_TITLE, title)
                                      putExtra(MusicService.EXTRA_ARTIST, artist)
                                  }
                                  if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.O) {
                                      startForegroundService(intent)
                                  } else {
                                      startService(intent)
                                  }
                                  result.success(null)
                              }
                              "stopService" -> {
                                  val intent = Intent(this, MusicService::class.java).apply {
                                      action = MusicService.ACTION_STOP
                                  }
                                  startService(intent)
                                  result.success(null)
                              }
                              "updateNotification" -> {
                                  val title = call.argument<String>("title") ?: ""
                                  val artist = call.argument<String>("artist") ?: ""
                                  val intent = Intent(this, MusicService::class.java).apply {
                                      action = MusicService.ACTION_START
                                      putExtra(MusicService.EXTRA_TITLE, title)
                                      putExtra(MusicService.EXTRA_ARTIST, artist)
                                  }
                                  if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.O) {
                                      startForegroundService(intent)
                                  } else {
                                      startService(intent)
                                  }
                                  result.success(null)
                              }
                              else -> result.notImplemented()
                          }
                      }
              }
          }
          KT

          # ── lib/main.dart ─────────────────────────────────────────────────────
          cat <<'DART' > lib/main.dart
          import 'dart:io';
          import 'package:flutter/material.dart';
          import 'package:flutter/services.dart';
          import 'package:audioplayers/audioplayers.dart';
          import 'package:on_audio_query/on_audio_query.dart';
          import 'package:permission_handler/permission_handler.dart';
          import 'package:google_fonts/google_fonts.dart';
          import 'package:device_info_plus/device_info_plus.dart';

          // ─────────────────────────────────────────────────────────────────────
          //  Music Service Bridge (Flutter ↔ Native Android)
          // ─────────────────────────────────────────────────────────────────────
          class MusicServiceBridge {
            static const _channel =
                MethodChannel('com.nareshkushmi.nkplayer/music_service');

            static Future<void> start(String title, String artist) async {
              try {
                await _channel.invokeMethod('startService', {
                  'title': title,
                  'artist': artist,
                });
              } catch (e) {
                debugPrint('MusicService start error: $e');
              }
            }

            static Future<void> update(String title, String artist) async {
              try {
                await _channel.invokeMethod('updateNotification', {
                  'title': title,
                  'artist': artist,
                });
              } catch (e) {
                debugPrint('MusicService update error: $e');
              }
            }

            static Future<void> stop() async {
              try {
                await _channel.invokeMethod('stopService');
              } catch (e) {
                debugPrint('MusicService stop error: $e');
              }
            }
          }

          // ─────────────────────────────────────────────────────────────────────
          //  Global Player Manager
          // ─────────────────────────────────────────────────────────────────────
          class PlayerManager {
            static final AudioPlayer player = AudioPlayer();
            static List<SongModel> playlist = [];
            static int currentIndex = 0;
          }

          // ─────────────────────────────────────────────────────────────────────
          //  Main
          // ─────────────────────────────────────────────────────────────────────
          void main() {
            WidgetsFlutterBinding.ensureInitialized();
            runApp(const NKPlayerApp());
          }

          class NKPlayerApp extends StatelessWidget {
            const NKPlayerApp({super.key});
            @override
            Widget build(BuildContext context) {
              return MaterialApp(
                title: 'NKPlayer',
                debugShowCheckedModeBanner: false,
                theme: ThemeData.dark().copyWith(
                  primaryColor: Colors.blueAccent,
                  scaffoldBackgroundColor: const Color(0xFF0F0F0F),
                ),
                home: const PermissionHandlerScreen(),
              );
            }
          }

          // ─────────────────────────────────────────────────────────────────────
          //  Permission Screen
          // ─────────────────────────────────────────────────────────────────────
          class PermissionHandlerScreen extends StatefulWidget {
            const PermissionHandlerScreen({super.key});
            @override
            State<PermissionHandlerScreen> createState() =>
                _PermissionHandlerScreenState();
          }

          class _PermissionHandlerScreenState
              extends State<PermissionHandlerScreen> {
            @override
            void initState() {
              super.initState();
              _checkPermissions();
            }

            Future<void> _checkPermissions() async {
              if (Platform.isAndroid) {
                final info = await DeviceInfoPlugin().androidInfo;
                final sdk = info.version.sdkInt;
                if (sdk >= 30) {
                  await Permission.manageExternalStorage.request();
                  if (sdk >= 33) {
                    await Permission.audio.request();
                    await Permission.notification.request();
                  }
                } else {
                  await Permission.storage.request();
                }
              }
              if (mounted) {
                Navigator.pushReplacement(
                  context,
                  MaterialPageRoute(builder: (_) => const FolderListScreen()),
                );
              }
            }

            @override
            Widget build(BuildContext context) =>
                const Scaffold(body: Center(child: CircularProgressIndicator()));
          }

          // ─────────────────────────────────────────────────────────────────────
          //  Data Model
          // ─────────────────────────────────────────────────────────────────────
          class CustomFolder {
            final String name;
            final List<SongModel> songs;
            CustomFolder({required this.name, required this.songs});
          }

          // ─────────────────────────────────────────────────────────────────────
          //  Folder List Screen
          // ─────────────────────────────────────────────────────────────────────
          class FolderListScreen extends StatefulWidget {
            const FolderListScreen({super.key});
            @override
            State<FolderListScreen> createState() => _FolderListScreenState();
          }

          class _FolderListScreenState extends State<FolderListScreen> {
            final TextEditingController _searchCtrl = TextEditingController();
            bool _isSearching = false;
            List<CustomFolder> _folders = [];
            List<SongModel> _allSongs = [];
            List<SongModel> _filteredSongs = [];
            bool _loading = true;

            @override
            void initState() {
              super.initState();
              _loadData();
              _searchCtrl.addListener(() {
                setState(() {
                  _filteredSongs = _allSongs
                      .where((s) => s.title
                          .toLowerCase()
                          .contains(_searchCtrl.text.toLowerCase()))
                      .toList();
                });
              });
            }

            Future<void> _loadData() async {
              final songs = await OnAudioQuery().querySongs();
              final Map<String, List<SongModel>> paths = {};
              for (var s in songs) {
                if (s.data.isEmpty) continue;
                paths.putIfAbsent(File(s.data).parent.path, () => []).add(s);
              }
              setState(() {
                _allSongs = songs;
                _folders = paths.entries
                    .map((e) => CustomFolder(
                        name: e.key.split('/').last, songs: e.value))
                    .toList();
                _loading = false;
              });
            }

            @override
            Widget build(BuildContext context) {
              return Scaffold(
                appBar: AppBar(
                  title: _isSearching
                      ? TextField(
                          controller: _searchCtrl,
                          autofocus: true,
                          decoration: InputDecoration(
                            hintText: 'Search all songs...',
                            border: InputBorder.none,
                            suffixIcon: _searchCtrl.text.isNotEmpty
                                ? Semantics(
                                    label: 'clear search button',
                                    button: true,
                                    excludeSemantics: true,
                                    child: IconButton(
                                        icon: const Icon(Icons.clear),
                                        onPressed: () => _searchCtrl.clear()),
                                  )
                                : null,
                          ),
                        )
                      : Text('Folders', style: GoogleFonts.poppins()),
                  actions: [
                    Semantics(
                      label: _isSearching ? 'close search button' : 'search button',
                      button: true,
                      excludeSemantics: true,
                      child: IconButton(
                        icon: Icon(_isSearching ? Icons.close : Icons.search),
                        onPressed: () => setState(() {
                          _isSearching = !_isSearching;
                          if (!_isSearching) _searchCtrl.clear();
                        }),
                      ),
                    ),
                  ],
                ),
                body: _loading
                    ? const Center(child: CircularProgressIndicator())
                    : _isSearching
                        ? ListView.builder(
                            itemCount: _filteredSongs.length,
                            itemBuilder: (_, i) => ListTile(
                              leading: const Icon(Icons.music_note,
                                  color: Colors.blueAccent),
                              title: Text(_filteredSongs[i].title, maxLines: 1),
                              onTap: () => Navigator.push(
                                  context,
                                  MaterialPageRoute(
                                      builder: (_) => PlayerScreen(
                                          songs: _filteredSongs,
                                          initialIndex: i))),
                            ),
                          )
                        : ListView.builder(
                            itemCount: _folders.length,
                            itemBuilder: (_, i) {
                              final f = _folders[i];
                              return ListTile(
                                leading: const Icon(Icons.folder,
                                    color: Colors.amber),
                                title: Text(f.name),
                                subtitle: Text('${f.songs.length} media files'),
                                onTap: () => Navigator.push(
                                    context,
                                    MaterialPageRoute(
                                        builder: (_) =>
                                            SongListScreen(folder: f))),
                              );
                            },
                          ),
              );
            }
          }

          // ─────────────────────────────────────────────────────────────────────
          //  Song List Screen
          // ─────────────────────────────────────────────────────────────────────
          class SongListScreen extends StatefulWidget {
            final CustomFolder folder;
            const SongListScreen({super.key, required this.folder});
            @override
            State<SongListScreen> createState() => _SongListScreenState();
          }

          class _SongListScreenState extends State<SongListScreen> {
            final TextEditingController _searchCtrl = TextEditingController();
            final Set<int> _selected = {};
            bool _isSel = false;
            List<SongModel> _filtered = [];

            @override
            void initState() {
              super.initState();
              _filtered = widget.folder.songs;
              _searchCtrl.addListener(() {
                setState(() {
                  _filtered = widget.folder.songs
                      .where((s) => s.title
                          .toLowerCase()
                          .contains(_searchCtrl.text.toLowerCase()))
                      .toList();
                });
              });
            }

            @override
            Widget build(BuildContext context) {
              return Scaffold(
                appBar: AppBar(
                  leading: _isSel
                      ? Semantics(
                          label: 'close selection button',
                          button: true,
                          excludeSemantics: true,
                          child: IconButton(
                              icon: const Icon(Icons.close),
                              onPressed: () => setState(
                                  () { _selected.clear(); _isSel = false; })),
                        )
                      : null,
                  title: _isSel
                      ? Text('${_selected.length} Selected')
                      : TextField(
                          controller: _searchCtrl,
                          decoration: InputDecoration(
                            hintText: 'Search in folder...',
                            border: InputBorder.none,
                            suffixIcon: _searchCtrl.text.isNotEmpty
                                ? Semantics(
                                    label: 'clear search button',
                                    button: true,
                                    excludeSemantics: true,
                                    child: IconButton(
                                        icon: const Icon(Icons.clear),
                                        onPressed: () => _searchCtrl.clear()),
                                  )
                                : null,
                          ),
                        ),
                  actions: _isSel
                      ? [
                          Semantics(
                            label: 'select all button',
                            button: true,
                            excludeSemantics: true,
                            child: IconButton(
                                icon: const Icon(Icons.select_all),
                                onPressed: () => setState(() => _selected
                                    .addAll(Iterable.generate(_filtered.length)))),
                          ),
                          Semantics(
                            label: 'play selected button',
                            button: true,
                            excludeSemantics: true,
                            child: IconButton(
                              icon: const Icon(Icons.play_circle),
                              onPressed: () => Navigator.push(
                                  context,
                                  MaterialPageRoute(
                                      builder: (_) => PlayerScreen(
                                          songs: _selected
                                              .map((i) => _filtered[i])
                                              .toList(),
                                          initialIndex: 0))),
                            ),
                          ),
                        ]
                      : null,
                ),
                body: ListView.builder(
                  itemCount: _filtered.length,
                  itemBuilder: (_, i) {
                    final isS = _selected.contains(i);
                    return ListTile(
                      tileColor:
                          isS ? Colors.blueAccent.withOpacity(0.1) : null,
                      leading: Icon(
                          isS ? Icons.check_circle : Icons.music_note,
                          color: i % 2 == 0 ? Colors.blueAccent : Colors.grey),
                      title: Text(_filtered[i].title, maxLines: 1),
                      onLongPress: () =>
                          setState(() { _selected.add(i); _isSel = true; }),
                      onTap: () {
                        if (_isSel) {
                          setState(() {
                            if (isS)
                              _selected.remove(i);
                            else
                              _selected.add(i);
                            _isSel = _selected.isNotEmpty;
                          });
                        } else {
                          Navigator.push(
                              context,
                              MaterialPageRoute(
                                  builder: (_) => PlayerScreen(
                                      songs: _filtered, initialIndex: i)));
                        }
                      },
                    );
                  },
                ),
              );
            }
          }

          // ─────────────────────────────────────────────────────────────────────
          //  Player Screen
          // ─────────────────────────────────────────────────────────────────────
          class PlayerScreen extends StatefulWidget {
            final List<SongModel> songs;
            final int initialIndex;
            const PlayerScreen(
                {super.key, required this.songs, required this.initialIndex});
            @override
            State<PlayerScreen> createState() => _PlayerScreenState();
          }

          class _PlayerScreenState extends State<PlayerScreen> {
            bool _playing = false;
            Duration _pos = Duration.zero;
            Duration _dur = Duration.zero;

            @override
            void initState() {
              super.initState();
              PlayerManager.playlist = widget.songs;
              PlayerManager.currentIndex = widget.initialIndex;

              PlayerManager.player.onPositionChanged
                  .listen((p) { if (mounted) setState(() => _pos = p); });
              PlayerManager.player.onDurationChanged
                  .listen((d) { if (mounted) setState(() => _dur = d); });
              PlayerManager.player.onPlayerComplete.listen((_) => _next());

              _playCurrent();
            }

            Future<void> _playCurrent() async {
              final song = PlayerManager.playlist[PlayerManager.currentIndex];
              try {
                await PlayerManager.player.play(DeviceFileSource(song.data));
                setState(() => _playing = true);
                // Start native foreground service → notification shows, music keeps playing
                await MusicServiceBridge.start(
                  song.title,
                  song.artist ?? 'Unknown Artist',
                );
              } catch (e) {
                debugPrint('Playback error: $e');
              }
            }

            void _next() {
              if (PlayerManager.currentIndex < PlayerManager.playlist.length - 1) {
                PlayerManager.currentIndex++;
                _playCurrent();
              } else {
                MusicServiceBridge.stop();
                setState(() => _playing = false);
              }
            }

            void _prev() {
              if (PlayerManager.currentIndex > 0) {
                PlayerManager.currentIndex--;
                _playCurrent();
              }
            }

            Future<void> _togglePlay() async {
              if (_playing) {
                await PlayerManager.player.pause();
              } else {
                await PlayerManager.player.resume();
              }
              setState(() => _playing = !_playing);
            }

            String _fmt(Duration d) =>
                d.toString().split('.').first.padLeft(8, '0').substring(2);

            @override
            Widget build(BuildContext context) {
              final song = PlayerManager.playlist[PlayerManager.currentIndex];
              return Scaffold(
                appBar: AppBar(title: const Text('Now Playing')),
                body: Column(
                  mainAxisAlignment: MainAxisAlignment.center,
                  children: [
                    const Icon(Icons.music_note,
                        size: 100, color: Colors.blueAccent),
                    const SizedBox(height: 30),
                    Padding(
                      padding: const EdgeInsets.symmetric(horizontal: 16),
                      child: Text(
                        song.title,
                        textAlign: TextAlign.center,
                        maxLines: 2,
                        style: GoogleFonts.poppins(
                            fontSize: 18, fontWeight: FontWeight.bold),
                      ),
                    ),
                    const SizedBox(height: 8),
                    Text(song.artist ?? 'Unknown Artist',
                        style: const TextStyle(color: Colors.grey, fontSize: 14)),
                    Slider(
                      value: _pos.inSeconds.toDouble().clamp(
                          0,
                          _dur.inSeconds.toDouble() > 0
                              ? _dur.inSeconds.toDouble()
                              : 1),
                      max: _dur.inSeconds.toDouble() > 0
                          ? _dur.inSeconds.toDouble()
                          : 1.0,
                      onChanged: (v) => PlayerManager.player
                          .seek(Duration(seconds: v.toInt())),
                    ),
                    Padding(
                      padding: const EdgeInsets.symmetric(horizontal: 20),
                      child: Row(
                        mainAxisAlignment: MainAxisAlignment.spaceBetween,
                        children: [
                          Text(_fmt(_pos),
                              style: const TextStyle(color: Colors.grey)),
                          Text(_fmt(_dur),
                              style: const TextStyle(color: Colors.grey)),
                        ],
                      ),
                    ),
                    const SizedBox(height: 20),
                    Row(
                      mainAxisAlignment: MainAxisAlignment.spaceEvenly,
                      children: [
                        Semantics(
                          label: 'previous button',
                          button: true,
                          excludeSemantics: true,
                          child: IconButton(
                              icon: const Icon(Icons.skip_previous, size: 45),
                              onPressed: _prev),
                        ),
                        Semantics(
                          label: _playing ? 'pause button' : 'play button',
                          button: true,
                          excludeSemantics: true,
                          child: FloatingActionButton.large(
                            backgroundColor: Colors.blueAccent,
                            onPressed: _togglePlay,
                            child: Icon(
                                _playing ? Icons.pause : Icons.play_arrow,
                                size: 50),
                          ),
                        ),
                        Semantics(
                          label: 'next button',
                          button: true,
                          excludeSemantics: true,
                          child: IconButton(
                              icon: const Icon(Icons.skip_next, size: 45),
                              onPressed: _next),
                        ),
                      ],
                    ),
                  ],
                ),
              );
            }
          }
          DART

      - name: Build APK
        run: |
          flutter pub get
          flutter build apk --release

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: NKPlayer-v1.0.52
          path: build/app/outputs/flutter-apk/app-release.apk
