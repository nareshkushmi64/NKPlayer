name: NKPlayer - Final Stability & Permission Fix
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.22.0'

      - name: Generate and Fix Project Structure
        run: |
          # 1. Force V2 Embedding structure
          rm -rf android/
          flutter create --org com.nareshkushmi --project-name nkplayer .
          
          # 2. Update pubspec.yaml (Version 1.0.9+19)
          cat <<EOF > pubspec.yaml
          name: nkplayer
          description: NKPlayer Pro - Ultimate Compatibility.
          version: 1.0.9+19
          publish_to: 'none'
          environment:
            sdk: '>=3.3.0 <4.0.0'
          dependencies:
            flutter:
              sdk: flutter
            just_audio: ^0.9.38
            audio_service: ^0.18.15
            on_audio_query: ^2.9.0
            permission_handler: ^11.3.0
            device_info_plus: ^10.1.0
            rxdart: ^0.27.7
          flutter:
            uses-material-design: true
          EOF

          # 3. Comprehensive Android Manifest
          cat <<EOF > android/app/src/main/AndroidManifest.xml
          <manifest xmlns:android="http://schemas.android.com/apk/res/android">
              <uses-permission android:name="android.permission.INTERNET"/>
              <uses-permission android:name="android.permission.RECORD_AUDIO" />
              <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
              <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
              <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
              <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
              <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="29" />

              <application
                  android:label="NKPlayer"
                  android:name="\${applicationName}"
                  android:icon="@mipmap/ic_launcher"
                  android:requestLegacyExternalStorage="true"
                  android:usesCleartextTraffic="true">
                  <activity
                      android:name="com.nareshkushmi.nkplayer.MainActivity"
                      android:exported="true"
                      android:launchMode="singleTop"
                      android:theme="@style/LaunchTheme"
                      android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
                      android:hardwareAccelerated="true"
                      android:windowSoftInputMode="adjustResize">
                      <intent-filter>
                          <action android:name="android.intent.action.MAIN"/>
                          <category android:name="android.intent.category.LAUNCHER"/>
                      </intent-filter>
                  </activity>
                  <service android:name="com.ryanheise.audioservice.AudioService"
                      android:foregroundServiceType="mediaPlayback"
                      android:exported="true">
                      <intent-filter>
                          <action android:name="android.intent.action.MEDIA_BUTTON" />
                      </intent-filter>
                  </service>
                  <meta-data android:name="flutterEmbedding" android:value="2" />
              </application>
          </manifest>
          EOF

          # 4. Corrected main.dart (Robust Permission Logic)
          cat <<'EOF' > lib/main.dart
          import 'dart:async';
          import 'dart:io';
          import 'package:flutter/material.dart';
          import 'package:just_audio/just_audio.dart';
          import 'package:audio_service/audio_service.dart';
          import 'package:on_audio_query/on_audio_query.dart';
          import 'package:permission_handler/permission_handler.dart';

          late AudioHandler _handler;
          final _query = OnAudioQuery();

          Future<void> main() async {
            WidgetsFlutterBinding.ensureInitialized();
            runApp(const MaterialApp(home: PermissionGate(), debugShowCheckedModeBanner: false));
          }

          class MyAudioHandler extends BaseAudioHandler with QueueHandler, SeekHandler {
            final _player = AudioPlayer();
            MyAudioHandler() {
              _player.playerStateStream.map(_transformEvent).pipe(playbackState);
              _player.currentIndexStream.listen((i) {
                if (i != null && queue.value.isNotEmpty) mediaItem.add(queue.value[i]);
              });
            }
            PlaybackState _transformEvent(PlayerState event) => PlaybackState(
              controls: [MediaControl.skipToPrevious, if (_player.playing) MediaControl.pause else MediaControl.play, MediaControl.skipToNext],
              systemActions: const {MediaAction.seek},
              androidCompactActionIndices: const [0, 1, 2],
              processingState: const {
                ProcessingState.idle: AudioProcessingState.idle,
                ProcessingState.loading: AudioProcessingState.loading,
                ProcessingState.buffering: AudioProcessingState.buffering,
                ProcessingState.ready: AudioProcessingState.ready,
                ProcessingState.completed: AudioProcessingState.completed,
              }[_player.processingState]!,
              playing: _player.playing,
              updatePosition: _player.position,
              bufferedPosition: _player.bufferedPosition,
            );
            @override Future<void> play() => _player.play();
            @override Future<void> pause() => _player.pause();
            @override Future<void> skipToNext() => _player.seekToNext();
            @override Future<void> skipToPrevious() => _player.seekToPrevious();
            @override Future<void> seek(Duration p) => _player.seek(p);

            Future<void> load(List<MediaItem> items, int index) async {
              final sources = items.map((i) => AudioSource.file(i.extras!['path'])).toList();
              queue.add(items);
              await _player.setAudioSource(ConcatenatingAudioSource(children: sources), initialIndex: index);
              _player.play();
            }
          }

          class PermissionGate extends StatefulWidget {
            const PermissionGate({super.key});
            @override State<PermissionGate> createState() => _PermissionGateState();
          }

          class _PermissionGateState extends State<PermissionGate> {
            bool _isChecking = true;
            String _log = "Initializing...";

            @override
            void initState() {
              super.initState();
              _initApp();
            }

            Future<void> _initApp() async {
              try {
                // 1. Initialize Audio Service first
                _handler = await AudioService.init(
                  builder: () => MyAudioHandler(),
                  config: const AudioServiceConfig(
                    androidNotificationChannelId: 'com.nareshkushmi.nkplayer.audio',
                    androidNotificationChannelName: 'NKPlayer Music',
                  ),
                );

                // 2. Request Permissions
                bool permissionStatus = await _query.permissionsStatus();
                if (!permissionStatus) {
                  permissionStatus = await _query.checkAndRequest();
                }

                if (permissionStatus) {
                  if (mounted) Navigator.pushReplacement(context, MaterialPageRoute(builder: (_) => const Folders()));
                } else {
                  setState(() { _log = "Storage permission denied. Please enable it in settings."; _isChecking = false; });
                }
              } catch (e) {
                setState(() { _log = "Startup Error: $e"; _isChecking = false; });
              }
            }

            @override
            Widget build(BuildContext context) {
              return Scaffold(
                backgroundColor: Colors.white,
                body: Center(
                  child: Padding(
                    padding: const EdgeInsets.all(20),
                    child: Column(
                      mainAxisAlignment: MainAxisAlignment.center,
                      children: [
                        const CircularProgressIndicator(),
                        const SizedBox(height: 20),
                        Text(_log, textAlign: TextAlign.center, style: const TextStyle(color: Colors.black54)),
                        if (!_isChecking) ...[
                          const SizedBox(height: 20),
                          ElevatedButton(onPressed: _initApp, child: const Text("Retry")),
                        ]
                      ],
                    ),
                  ),
                ),
              );
            }
          }

          class Folders extends StatelessWidget {
            const Folders({super.key});
            @override Widget build(BuildContext context) {
              return Scaffold(
                appBar: AppBar(title: const Text("NKPlayer"), centerTitle: true, elevation: 0),
                body: FutureBuilder<List<AlbumModel>>(
                  future: _query.queryAlbums(),
                  builder: (context, snap) {
                    if (snap.connectionState == ConnectionState.waiting) return const Center(child: CircularProgressIndicator());
                    if (!snap.hasData || snap.data!.isEmpty) return const Center(child: Text("No Music Folders Found"));
                    return ListView.builder(
                      itemCount: snap.data!.length,
                      itemBuilder: (context, i) => ListTile(
                        leading: const Icon(Icons.folder, color: Colors.orange, size: 40),
                        title: Text(snap.data![i].album),
                        subtitle: Text("${snap.data![i].numOfSongs} Songs"),
                        onTap: () => Navigator.push(context, MaterialPageRoute(builder: (_) => SongsPage(album: snap.data![i]))),
                      ),
                    );
                  },
                ),
              );
            }
          }

          class SongsPage extends StatelessWidget {
            final AlbumModel album;
            const SongsPage({super.key, required this.album});
            @override Widget build(BuildContext context) {
              return Scaffold(
                appBar: AppBar(title: Text(album.album)),
                body: FutureBuilder<List<SongModel>>(
                  future: _query.queryAudiosFrom(AudiosFromType.ALBUM_ID, album.id),
                  builder: (context, snap) {
                    if (!snap.hasData) return const Center(child: CircularProgressIndicator());
                    final songs = snap.data!;
                    return ListView.builder(
                      itemCount: songs.length,
                      itemBuilder: (context, i) => ListTile(
                        leading: const Icon(Icons.music_note, color: Colors.blue),
                        title: Text(songs[i].title, maxLines: 1),
                        subtitle: Text(songs[i].artist ?? "Unknown Artist"),
                        onTap: () => Navigator.push(context, MaterialPageRoute(builder: (_) => Player(songs: songs, index: i))),
                      ),
                    );
                  },
                ),
              );
            }
          }

          class Player extends StatelessWidget {
            final List<SongModel> songs;
            final int index;
            const Player({super.key, required this.songs, required this.index});
            @override Widget build(BuildContext context) {
              final items = songs.map((s) => MediaItem(id: s.id.toString(), title: s.title, artist: s.artist, extras: {'path': s.data})).toList();
              (_handler as MyAudioHandler).load(items, index);
              return Scaffold(
                appBar: AppBar(title: const Text("Now Playing")),
                body: StreamBuilder<MediaItem?>(
                  stream: _handler.mediaItem,
                  builder: (context, snap) {
                    final item = snap.data;
                    if (item == null) return const Center(child: CircularProgressIndicator());
                    return Column(
                      mainAxisAlignment: MainAxisAlignment.center,
                      children: [
                        const Icon(Icons.music_note, size: 120, color: Colors.blueAccent),
                        Padding(padding: const EdgeInsets.all(30), child: Text(item.title, style: const TextStyle(fontSize: 18, fontWeight: FontWeight.bold), textAlign: TextAlign.center)),
                        Row(
                          mainAxisAlignment: MainAxisAlignment.spaceEvenly,
                          children: [
                            IconButton(icon: const Icon(Icons.skip_previous, size: 50), onPressed: _handler.skipToPrevious),
                            StreamBuilder<PlaybackState>(
                              stream: _handler.playbackState,
                              builder: (context, pSnap) {
                                final playing = pSnap.data?.playing ?? false;
                                return IconButton(icon: Icon(playing ? Icons.pause_circle : Icons.play_circle, size: 80, color: Colors.blue), onPressed: playing ? _handler.pause : _handler.play);
                              },
                            ),
                            IconButton(icon: const Icon(Icons.skip_next, size: 50), onPressed: _handler.skipToNext),
                          ],
                        ),
                      ],
                    );
                  },
                ),
              );
            }
          }
          EOF

      - name: Build APK
        run: |
          flutter pub get
          flutter build apk --release

      - name: Upload NKPlayer APK
        uses: actions/upload-artifact@v4
        with:
          name: NKPlayer-v1.0.9-Pro-Final-Check
          path: build/app/outputs/flutter-apk/app-release.apk
